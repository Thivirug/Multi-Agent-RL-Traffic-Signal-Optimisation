1) Create the virtual env 
    E.g. python -m venv sumoenv (**Use sumoenv as the name becz the gitignore uses that name otherwise there will be a lot of commits**)
2) Activate the venv
3) pip install -r requirements.txt
   
